import template from './temperature.html';
import controller from './temperature.controller.js';

const TemperatureComponent = {
  template,
  controller,
  bindings: {},
};

export default TemperatureComponent;
